# Python Specific Instructions

- Use `venv` or `env` for dependency management.
- Follow PEP 8 style guidelines.
- Prefer `pathlib` over `os.path`.
- When suggested fixes involve Odoo, ensure XML and Python models are synchronized.
- Use `ast` for code analysis when possible.
- Always include type hints in new functions.
