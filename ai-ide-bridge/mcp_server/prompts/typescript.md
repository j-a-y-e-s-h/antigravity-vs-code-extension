# TypeScript/JavaScript Specific Instructions

- Use `npm` or `yarn` for dependency management.
- Prefer `ESLint` and `Prettier` for styling.
- Use `async/await` over callbacks when possible.
- When suggested fixes involve VS Code extensions, follow the `vscode` API best practices.
- Ensure `jest` or `mocha` tests are updated for structural changes.
