# Dart/Flutter Specific Instructions

- Use `flutter pub get` for dependency management.
- Follow the official Dart style guide.
- Prefer `Riverpod` for state management if applicable.
- Ensure `widget` tests are prioritized for UI changes.
- Use `dart format` for code formatting.
- When suggested fixes involve Flutter, ensure `Material 3` is used for modern UI.
